import React from "react";
import { HeaderContainer, HeaderTitle, HeaderDesc } from "./HeaderElements";

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>The best care for everyone!</HeaderTitle>
      <HeaderDesc>Life-changing and 24/7 Healthcare</HeaderDesc>
    </HeaderContainer>
  );
};

export default Header;
